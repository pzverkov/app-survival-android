name: Nightly Security Scan

on:
  schedule:
    - cron: "17 2 * * *" # 02:17 UTC daily
  workflow_dispatch:
  pull_request:
    branches: [ main ]

permissions:
  actions: read
  contents: write
  pull-requests: write
  security-events: write

jobs:
  # NOTE: google/osv-scanner-action provides reusable workflows (not a step-level action).
  # We run it as a separate job and gate downstream logic based on the job result.
  # Reusable workflows can be pinned by SHA via the `@<sha>` syntax just like
  # step-level actions; the trailing comment records the human-readable tag.
  osv-scan:
    name: OSV scan
    uses: "google/osv-scanner-action/.github/workflows/osv-scanner-reusable.yml@c51854704019a247608d928f370c98740469d4b5" # v2.3.5
    permissions:
      actions: read
      contents: read
      security-events: write
    with:
      # Keep args explicit so behavior is stable across OSV versions.
      scan-args: |-
        --recursive
        --skip-git
        ./
      # This repo already has a separate CodeQL workflow; don't upload SARIF here.
      upload-sarif: false
      # Let the job fail if vulnerabilities are found; downstream job handles reporting/PR.
      fail-on-vuln: true

  security-scan:
    needs: [osv-scan]
    if: always()
    runs-on: ubuntu-latest

    steps:
      - name: Harden runner
        uses: step-security/harden-runner@8d3c67de8e2fe68ef647c8db1e6a09f647780f40 # v2.19.0
        with:
          egress-policy: audit

      - name: Checkout
        uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Setup Node
        uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          # Node 24 is the active LTS as of October 2025; 24.15.0 shipped 2026-04-15.
          node-version: "24.15.0"
          cache: "npm"

      - name: Install
        run: npm ci

      # Run npm audit, but keep the workflow running so we can open/update a PR when needed.
      - name: npm audit (prod deps, high+)
        id: npm_audit
        shell: bash
        run: |
          set -euo pipefail
          npm audit --omit=dev --audit-level=high --json > npm-audit.json || true

          node - <<'NODE'
          const fs = require('fs');
          const data = JSON.parse(fs.readFileSync('npm-audit.json', 'utf8'));

          // npm may output {"error": ...} on some failures (registry/network/etc).
          const auditError = !!data.error;

          let hasVulns = false;
          if (!auditError) {
            const v = (data.metadata && data.metadata.vulnerabilities) || {};
            const high = Number(v.high || 0);
            const critical = Number(v.critical || 0);
            hasVulns = (high + critical) > 0;
          }

          fs.appendFileSync(process.env.GITHUB_OUTPUT, `audit_error=${auditError}\nhas_vulns=${hasVulns}\n`);
          NODE

      - name: Decide if findings exist
        id: gate
        shell: bash
        run: |
          set -euo pipefail
          HAS="false"

          if [ "${{ steps.npm_audit.outputs.has_vulns }}" = "true" ]; then
            HAS="true"
          fi

          # OSV job fails on vulnerabilities (or errors). Treat any failure as a finding.
          if [ "${{ needs.osv-scan.result }}" = "failure" ]; then
            HAS="true"
          fi

          echo "has_findings=$HAS" >> "$GITHUB_OUTPUT"

      - name: Update README stamp (only on findings; schedule/dispatch only)
        if: github.event_name != 'pull_request' && steps.gate.outputs.has_findings == 'true'
        shell: bash
        run: |
          set -euo pipefail
          TS="$(date -u +'%Y-%m-%d %H:%M UTC')"

          perl -0777 -i -pe "s/<!-- security-check:begin -->.*?<!-- security-check:end -->/<!-- security-check:begin -->\nLast automated security check: **$TS** — **FAIL**\n<!-- security-check:end -->/s" README.md

      - name: Create/Update PR (only on findings; schedule/dispatch only)
        if: github.event_name != 'pull_request' && steps.gate.outputs.has_findings == 'true'
        uses: peter-evans/create-pull-request@5f6978faf089d4d20b00c7766989d076bb2fc7f1 # v8.1.1
        with:
          branch: chore/security-findings
          delete-branch: false
          title: "chore(security): nightly scan found findings"
          commit-message: "chore(security): record nightly scan findings"
          body: |
            Nightly security scan detected findings.

            - npm audit (prod deps, high+)
            - OSV scanner

            Please review the workflow logs and remediate.

      - name: Fail job if findings exist
        if: steps.gate.outputs.has_findings == 'true'
        run: exit 1
